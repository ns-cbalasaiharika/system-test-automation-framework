# =============================================================================
# PRODUCTION ENVIRONMENT - READ ONLY!
# ⚠️  WARNING: NEVER run write operations against production!
# Usage: Only for read-only health checks
# =============================================================================

name: production
description: Production - READ-ONLY scenarios only!

readOnly: true                                       # NOT YET ENFORCED - be careful!

# Production service URLs in 'prod' namespace
services:
  client-oppy-configuration: http://client-oppy-configuration.prod.svc:6010
  client-oppy-steering: http://client-oppy-steering.prod.svc:6020
  client-oppy-orchestrator: http://client-oppy-orchestrator.prod.svc:6030
  addonman: http://addonman.prod.svc:8080
  downloader: http://downloader.prod.svc:8081
  device-classification: http://device-classification.prod.svc:8082

defaults:
  tenantId: prod-readonly-tenant                     # Read-only tenant with minimal permissions
  
  headers:
    Content-Type: application/json
    x-netskope-tenantid: prod-readonly-tenant
    x-netskope-user-email: k6-perf-test@netskope.com
  
  thinkTime:                                         # Conservative - minimize prod impact
    minMs: 1000
    maxMs: 3000
